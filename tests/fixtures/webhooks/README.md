# Webhook fixtures

Sanitized, representative GitHub App webhook payloads (fields trimmed to
what CommitGuard reads plus some realistic noise). IDs, logins, SHAs and URLs
are fictitious. They contain no secrets, tokens or signatures; tests sign them
with a throwaway secret at run time.
